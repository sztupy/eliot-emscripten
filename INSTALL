Build instructions on OSX (ARM), using homebrew as the package manager

## Native build

```bash
brew install boost

./bootstrap
CPPFLAGS="-I/opt/homebrew/include" LDFLAGS="-L/opt/homebrew/lib" ./configure
make
```

Run `utils/eliottxt` as the app

## Emscripten build

```bash
brew install emscripten
brew install boost

./bootstrap
CPPFLAGS="-I/opt/homebrew/include" LDFLAGS="-L/opt/homebrew/lib" emconfigure ./configure
emmake make
```
